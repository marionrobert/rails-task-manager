<h1><%=@task.title%></h1>
<%= link_to("Edit the task", edit_path(@task.id)) %>

<h3><%= @task.details%></h3>

<% if @task.completed == false %>
  <p>This task is not completed yet</p>
<%else %>
  <p>This task is completed </p>
<%end %>


<%= link_to("Back to list", tasks_path) %>
